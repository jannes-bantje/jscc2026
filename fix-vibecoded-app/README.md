# fix vibecoded app (WearOS)

Hosted by @Narigo

- use a different model to fix it (and the latest one of it)
- better understanding the existing code-base:
  - use `/init` to create an overview about the project
  - You can tell to create (mermaid) diagrams
- tackle one package after another
- use a new context (chat) window for different packages
- ask for a list of features
- later, work on the features individually and use `/clear` between
